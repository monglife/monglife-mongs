# stage 회귀 테스트 — 배포 후 (2026-09-20 14:19 ~ 14:29 KST)

지난 통합테스트 32건 중 **이번 재검증에서 빠져 있던 16건**을 돌렸다.
계정 `3` · 몽 `3`. 앱 액세스 토큰으로 사용자 API 를 직접 호출하고, 관리자 조회는 관리자 웹으로 대조했다.

## 요약

| 구분 | 결과 |
|---|---|
| R-01 ~ R-14 | **전부 통과** |
| 신규 결함 | 없음 |
| 미실행 | G-11 도감, G-13 슬롯·몽 삭제, A-02b DEAD 복구 (원래도 범위 밖) |

---

## R-01 · 수면·기상 스케줄 교체 (G-07) — **최우선 회귀 지점**

이번 배포가 `MongSchedulerType` 파서를 통째로 바꿨고, 재우기/깨우기가 그 파서로 스케줄을 지우고 만든다. 고아 스케줄이 생기면 몽이 영영 안 깨거나 지수가 안 줄어든다.

| 단계 | 스케줄 | `isSleep` |
|---|---|---|
| 기준 | `34:DECREASE-STATUS` `35:INCREASE-POOP` `36:SLEEP` `37:WAKEUP` | false |
| 재우기 | 34·35 **제거** → `39:INCREASE-STATUS` 생성 | true |
| 깨우기 | 39 제거 → `40:DECREASE-STATUS` `41:INCREASE-POOP` **복원** | false |

전부 `isScheduled=true`, 고아 0건, 4건으로 원복. 지난 통합테스트 G-07 과 동일한 거동.

---

## R-02 ~ R-05 · 미션 적립 로직

### R-02 COUNT (M-04)

`MS_D_014 랜덤 뽑기 3번` — 0 → 1 → 2 → **3/3 `CLAIMABLE`**. 목표치 도달에서 정확히 멈춘다.

`MS_D_009 배변 2번 치우기` 는 이미 2/2 `CLAIMABLE` 인 상태에서 배변 4개를 더 치웠는데 **2/2 에서 불변**. 초과 적립 없음.

### R-03 DISTINCT (M-05)

`MS_W_005 서로 다른 아이템 4종 사용하기` 로 확인.

| 사용 | 결과 |
|---|---|
| `FD000` (id 7) | 0 → **1** |
| `FD000` (id 8) — 같은 코드 | **1 에서 불변** |
| `FD010` (id 9) — 다른 코드 | 1 → **2** |

훈련에서도 같다. `MS_W_003 모든 종류의 훈련` — `TR000` 두 번은 1 에서 멈추고, `TR001` 을 추가해야 2 가 된다.

### R-04 ACCUMULATE (M-06)

| 미션 | 추이 | 근거 |
|---|---|---|
| `MS_W_006/007` 페이 포인트 쓰기 | 30 → 130 → 230 → **330** | 티켓 100원 × 3 |
| `MS_M_009` 훈련 점수 누적 | 0 → 1 → 26 → **46** | score 1·25·20 그대로 |
| `MS_M_008` 배변 치우기 | 2 → **6** | 배변 4개 = 개수만큼 |
| `MS_M_005/006` 경험치 | 9 → 17 → 25 → 35 → **43** | 훈련 +8, 배변 +8 |
| `MS_W_008` 페이 포인트 벌기 | 0 → 70 → **170** | **성공한 훈련만** |

`MS_W_008` 이 훈련 실패 때 안 오르는 것도 그대로다. 증가분이 0 이라 `accumulate` 가 건너뛴다.

### R-05 CARE_DAY (M-07)

오늘 12건 넘는 액션(먹이기·인벤토리·뽑기·훈련·배변·재우기·진화)을 했지만 `MS_W_009/010` 은 **1 에서 불변**. 하루 1회 규칙 유지.

---

## R-06 / R-07 · 미션 선정·노출

### R-06 일간 선정 불변식 (M-02)

정확히 **5건** — `MS_D_002` `MS_D_005` `MS_D_009` `MS_D_014` `MS_D_019`.
액션은 `FEED_FOOD` `FEED_SNACK` `POOP_CLEAN` `RANDOM_DRAW` `SLEEP` 로 중복 없음.

### R-07 로테이션 그룹 노출 (M-03)

| | 값 |
|---|---|
| `currentRotationGroup` | **0** |
| 게시 중 | `MS_W_001~010` · `MS_M_001~010` (`isPublished: true`) |
| 미게시 | `MS_W_011~020` · `MS_M_011~020` (`isPublished: false`) |
| 앱이 받은 목록 | 주간 `MS_W_001~010`, 월간 `MS_M_001~010` — **일치** |

> 관리자 웹은 "이번 주기는 **그룹 1** 차례입니다" 로 쓴다. 결함이 아니다. 필터 드롭다운이
> `그룹 1 = value 0`, `그룹 2 = value 1` 로 **1부터 세는 표기**를 쓴다. 내부 `rotationGroup 0` 과 같은 것이다.

**M-12 기대값이 여기서 확정된다.** 오늘 밤 00:00 에 `currentRotationGroup` 이 **1** 로 넘어가고, `MS_W_011~020` 이 게시 중으로, `MS_W_001~010` 이 미게시로 뒤집혀야 한다. 주기는 `09-21 ~ 09-27`.

---

## R-08 · 게시 잠금 (M-09)

게시 중인 `MS_W_001` 에 강제 호출:

| 요청 | 결과 |
|---|---|
| `PUT /admin/missions/{id}` | **400 `400-102-005`** |
| `DELETE /admin/missions/{id}` | **400 `400-102-005`** |

"게시 중인 미션은 수정·삭제할 수 없습니다."

---

## R-09 ~ R-12 · 게임 플레이

| # | 케이스 | 결과 |
|---|---|---|
| R-09 | 음식 섭취 (G-02) | `FD000` → `MONGS-CHARACTER-MONG-005`, payPoint **−10**, satiety **+5**, weight **+10** |
| R-10 | 배변 치우기 (G-04) | 4개 → 0, exp **+8**(개당 2), `MS_M_008` +4 |
| R-11 | 훈련 (G-05) | 아래 표 |
| R-12 | 뽑기·티켓 (G-09) | 티켓 `MONGS-CHARACTER-MONG-009` → 뽑기 `MONGS-CHARACTER-MONG-010`, 3회 모두. `MS_W_004` DISTINCT 0→3 (서로 다른 상품 3종) |

### R-11 훈련 두 경로

| 경로 | 입력 | 결과 |
|---|---|---|
| 실패 | `TR000` score **1** (기준 20) | `isSuccess: false`, payPoint **불변**, `MS_M_009` +1, `MS_W_008` **0 유지** |
| 성공 | `TR000` score **25** | `isSuccess: true`, payPoint **+70**, `MS_W_008` 0 → **70** |
| 성공(다른 종목) | `TR001` score 20 | payPoint +100, `MS_W_003` DISTINCT 1 → **2** |

### 곁가지 — 음식 거절 코드

satiety 가 100 에 닿으면 `FD000` 재섭취가 거절된다. 거절 자체는 정상인데 **코드가 `500-101-000` 인데 `httpStatus` 는 400** 이다. 코드 접두사와 실제 상태가 어긋난다. 이번 배포와 무관한 기존 문제이고 동작에는 영향이 없다.

---

## R-13 · 관리자 지수 변경 → 앱 MQTT (A-08)

`PATCH /admin/mongs/3/status {satiety:61, strength:62, poopCount:4}` → 200.

약 10초 안에 `mongs-stg/mong/management/3` 로 `MONGS-CHARACTER-MONG-500` 수신:
`strengthRatio=62.0, satietyRatio=61.0, poopCount=4.0` — 보낸 값 그대로.

---

## R-14 · 진화 (G-06)

| 단계 | 결과 |
|---|---|
| 관리자로 exp 100 | `stateCode` 가 **자동으로 `EVOLUTION_READY`** 로 전이 |
| 앱 진화 호출 | `MONGS-CHARACTER-MONG-018`, `CH100 별몽` → **`CH210 무난한 별별몽` Lv.2**, exp 0 |
| MQTT | `mongCode=CH210, mongName=무난한 별별몽` 푸시 수신 |
| 화면 | 새 스프라이트 **정상 렌더** (실기기 확인) |
| 미션 | `MS_M_002 몽 3번 진화` 1→2, `MS_M_007 서로 다른 몽 3종` 1→**2** |

---

## 테스트가 바꾼 stage 상태 (재검증분에 이어서)

| 대상 | 변화 |
|---|---|
| 몽 3 | `CH100 별몽 Lv.1` → **`CH210 무난한 별별몽 Lv.2`**, exp 0 |
| 몽 3 페이 포인트 | 1,400 → **1,240** |
| 몽 3 인벤토리 | 5개 지급 후 3개 사용 → 2개 잔여 (`FD011`, `SN000`) |
| 계정 3 미션 | `MS_D_009` `MS_D_014` `MS_D_019` **`CLAIMABLE`**, `MS_M_001` `CLAIMED` |
| 스케줄 | taskId 34·35 → 40·41 로 교체 (재우기/깨우기 왕복) |

미션 마스터 데이터는 안 건드렸다. R-08 의 강제 PUT/DELETE 는 둘 다 반려됐다.
